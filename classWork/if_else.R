##If, If Else
if(<condition>) {
  ##do something
} else { ## does not need the else, can just do if
  ##do something else
}

if(<condition1>) {
  ##do somehting 
} else if(<condition2>) { ## any number of else ifs, but the else has to be at the end
  ## do something different\
} else {
  ##do something different
}